from setuptools import setup

setup(
    name="opencv-contrib-python",
    version="4.11.0.86",
    description="Metadata-only shim so MediaPipe can use headless OpenCV on Streamlit Cloud.",
    py_modules=[],
)
